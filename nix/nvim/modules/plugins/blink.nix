{
  # blink.cmp: intentionally NOT ported yet. You are undecided on its config.
  # This file is not imported (see plugins/default.nix). When ready, port from
  # plugin/20_plugins.lua blink block into plugins.blink-cmp.settings, and add
  # the completion keymap split (blink owns C-n/p/y/space/e/k; Tab/C-l/C-h stay
  # LuaSnip via preset = "none").
  #
  # Until then there is no completion menu engine active.
}
