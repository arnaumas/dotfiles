{
  # lz.n lazy loader: NOT part of the faithful port. Your current config loads
  # everything eagerly (init.lua vim.pack.add), so there is nothing to lazy-load
  # yet. This file is not imported (see plugins/default.nix). It stays as a marker
  # for the future lazy-loading layer discussed earlier (per-plugin lazyLoad
  # triggers), to add on top once the eager port builds cleanly.
}
